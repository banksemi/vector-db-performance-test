services:
  pgvector:
    image: pgvector/pgvector:pg16
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 1s
      timeout: 5s
      retries: 5
    ports:
      - 127.0.0.1:5432:5432
    env_file:
      - config.env